
\documentclass[12pt]{report}
\input{bayesuvius.sty}
\begin{document}
\chapter{Green's Functions for PDE}
\label{ch-green-fun-pde}


\section{Laplace Equation (Hyperbolic)}

Poisson Equation: $\nabla^2 \eta=S$

Laplace Equation: $\nabla^2 \eta=0$

\subsection{Notation}

1-dim

$\max(x, x')=$ ramp with height $x$ when plotted as a
function of $x'$.

\beq
\partial_x\min(x,x')=
\left\{
\begin{array}{ll}
\partial_x x & \text{if $x<x'$}
\\
\partial_x x' & \text{if $x>x'$}
\end{array}
\right\}
=\indi(x<x')
\eeq

\beq
1=\int_{-\infty}^\infty dx\;\partial_x\indi(x<x')=\int_{-\infty}^\infty dx\; \delta(x-x')
\eeq
so
\beq
 \partial_x\indi(x<x')=\delta(x-x')
\eeq

Define
\beq
L_{x}= - \partial_x^2,\quad
L_{x} G(x,x')=\delta(x-x')
\eeq





2-dim

$\vec{r}=(x,y)=x\hat{x} + y\hat{y}$, \quad $\nabla =(\partial_x, \partial_y)$,
 $\nabla^2=\partial_x^2 + \partial_y^2$

\beq
\engdim{\partial_x^2}\engdim{G}=
\engdim{\delta(x)}^d
\implies 
\engdim{G}=L^{2-d}
\eeq

$r=|\vec{x}-\vec{x'}|$
\begin{itemize}

\item $d=1$: $G\sim r$, grows with $r$
\item $d=2$: $G\sim \ln(r)$
\item $d=3$: $G\sim 1/r$, decreases with $r$
\end{itemize}

\begin{figure}[h!]
\centering
%\includegraphics[width=5in]
%{green-fun-pde/distance-funs.png}
\caption{Plots of various Green's functions for the Laplace Equation
in 1 dim.
 }
\label{fig-distance-funs}
\end{figure}

assume $x>0$
\beq
|x-x'|=
\left\{
\begin{array}{ll}
-(x-x')&\text{if $x'>x$}
\\
x-x'&\text{if $-x<x'<x$}
\\
x-x'&\text{if $x'<-x$}
\end{array}
\right\},\quad
|x+x'|=
\left\{
\begin{array}{ll}
x+x'&\text{if $x'>x$}
\\
x'+x&\text{if $-x<x'<x$}
\\
-(x+x')&\text{if $x'<-x$}
\end{array}
\right\}
\eeq

\beq
\frac{1}{2}|x-x'|
+\frac{1}{2}|x+x'|
=
\underbrace{\left\{
\begin{array}{ll}
x'&\text{if $x'>x$}
\\
x&\text{if $-x<x'<x$}
\\
-x'&\text{if $x'<-x$}
\end{array}
\right\}}_{
=\max(x,x') \text{ if $x'>0$}}
\eeq

\beq
\frac{1}{2}|x-x'|
-\frac{1}{2}|x+x'|
=
\underbrace{\left\{
\begin{array}{ll}
-x&\text{if $x'>x$}
\\
-x'&\text{if $-x<x'<x$}
\\
x&\text{if $x'<-x$}
\end{array}
\right\}}_{
=-\min(x,x') \text{ if $x'>0$}}
\eeq



 

\subsection{General Solution}


\begin{claim}
Suppose
\beq
L_{x}= -\partial_x^2,\quad
L_{x} G(x, x')=\delta(x-x')
\eeq
The solution to the equation
\beq
L_{x} \eta (x) = S(x)\quad
\eeq
for $x>0$
with  Dirichlet, Neumann or Robin boundary conditions satisfies the {\bf general solution}:

\beqa
\ket{\eta}
&=&
\underbrace{G{P_\rvx}\ket{S}}_{\text{Source Term $\cala_S$}}
\\
&&+\underbrace{
G\left[-\ket{x'}\partial_{x'}\bra{x'}
+
\ket{x'}\cev{\partial}_{x'}\bra{x'}\right]_{x'=0}
\ket{\eta}}_{\text{Boundary Conditions (BC) term $\cala_{BC}$}}
\eeqa
or, in non-Dirac notation,

\beqa
\eta(x)
&=&
\int_0^\infty dx'\;G(x,x')S(x')
\\
&&+
G(x,0) \left[-\partial_{x'}
+ \cev{\partial}_{x'}
\right]\eta(0)
\eeqa
\end{claim}
\proof
\qed


\subsection{Point Sources and their images}


Free space Geeen's function

1-dim
\beq
G^+(x, x')= -\;\frac{1}{2}|x-x'| = g_0(x-x')
\eeq
2-dim
\beq
G^+(\vec{r},\vec{r}\;')= -\;\frac{1}{2\pi}\ln |\vec{r}
-\vec{r}\;'| = g_0(\vec{r}-\vec{r}\;')
\eeq
3-dim
\beq
G^+(\vec{r},\vec{r}\;')= -\;\frac{1}{4\pi |\vec{r}
-\vec{r}\;'|} = g_0(\vec{r}-\vec{r}\;')
\eeq


\beq
G^-(x, x')=G(-x, x')
\eeq






\subsection{Dirichlet BC}

\beq
G^D(x, x')= G^+ -G^-
=\min(x, x')
\eeq

$\min(x,x')$ has slope$+1$  for $x'\in [-x, x]$
and is constant for $x'<-x$ and $x'>x$.
This kink at $x'=\pm x$
leads to a jump in the first derivative $\partial_{x'}G$ and to a delta in the 
second derivative $\partial_{x'}^2G$.
Hence $L G=\delta(x-x') + \delta(x+x')$
Since we are working in the $x'>0$ quadrant,
the mirror image delta function $\delta(x+x')$ can be 
ignored.


\subsection{Neumann BC}
\beq
G^N(x, x')=G^++G^- =
-\max(x, x')
\eeq

$\max(x,x')$ is constant  for $x'\in [-x, x]$
and has slope $\pm 1$ for $x'<-x$ and $x'>x$.
This kink at $x'=\pm x$
leads to a jump in the first derivative $\partial_{x'}G$ and to a delta in the 
second derivative $\partial_{x'}^2G$.
Hence $L G=\delta(x-x') + \delta(x+x')$
Since we are working in the $x'>0$ quadrant,
the mirror image delta function $\delta(x+x')$ can be 
ignored.


\subsection{Mixed (Robin)}

\beq
G^R= G^+ - G^- +
\underbrace{2k\int_0^\infty 
dX\;e^{-k X} g_0(x+x'+X)}_{J}
\label{eq-robin-superposition-X}
\eeq
where $k= \frac{a_1}{a_0}$.


\begin{claim}
According to  Eq.(\ref{eq-robin-superposition-X}),

\beq 
G^D= G^+ - G^- = \min (x,x')
\eeq
\beq
G^N=-\max(x, x') + \frac{1}{k}
\eeq
\end{claim}
\proof


\beq
\int dx\;xe^{ax}
=
\frac{e^{ax}}{a}\left(x-\frac{1}{a}\right)
\eeq

 $X_1=x+x'>0$

\begin{align}
J&=
-k\int_0^\infty 
dX\;e^{-k X}
|X+X_1|
\\
&=
-k\int_{X_1}^{\infty}
dX\;e^{-k (X-X_1)}X
\\
&=
-e^{k X_1}e^{-k X_1}\left(X_1+\frac{1}{k}\right)
\\
&=-\left(X_1+\frac{1}{k}\right)
\end{align}

\qed










Free space Green's function
\beq
G_0\twoevent{x}{t}{x'}{t'}=
g_0(x-x', t-t)
\eeq

\beq
G_\mu\twoevent{x}{t}{x'}{t'}=
g_\mu(x, x'; t-t) \quad\text{for $\mu = D, N, R$}
\eeq


$\tau=t-t'>0$
\beq
\call[g_0(x-x', \cdot)](s)=
\tilde{g}_0(x-x',s)]=\int_0^\infty d\tau\; e^{-s\tau}g_0(x-x', \tau)
\eeq

\beq
\call[g_\mu(x,x'; \cdot)](s)=
\tilde{g}_\mu(x, x';s)]=\int_0^\infty d\tau\; e^{-s\tau}g_\mu(x-x'; \tau)
\quad\text{for $\mu = D, N, R$}
\eeq

Diffusion
\beq
K= \sqrt{\frac{s}{D}}
\eeq
wave
\beq
K=\frac{s}{c}=s
\eeq

\beq 
K_0= \frac{a_0}{a_1}
\eeq

robin
\beq
[a_0 + a_1\partial_x]\eta(x,t)=0
\eeq




\begin{claim}
\beq
\tilde{g}_R(x, x';s)=
\tilde{g}_0(x-x', s)
+\tilde{R}(s)\tilde{g}_0(x+x', s)
\eeq

\beq
\tilde{g}_0(x-x', s)=
\frac{1}{2DK}
e^{-K|x-x'|}
\eeq



\beq
\tilde{R}(s)=
\frac{K+K_0}{K-K_0}
\eeq

$R(\tau)=$ boundary response kernel
Hence, in the time domain, 
\beq
G^R= g_0(x-x', \tau)
+
\underbrace{\int_0^{\tau}
d\tau'\; R(\tau')
g_0(x+x', \tau-\tau')}_{\text{time convolution}}
\eeq
\end{claim}
\proof

\beq
(\partial_t - D\partial_x^2)g(x, x'; t)=\delta(x-x')\delta(t-t')
\eeq
Apply 
$\int_0^\infty dt\;e^{-st}$
to both sides and integrate time derivatives by parts


\beq
(\partial_x^2-K^2)
\tilde{g}(x, x';s)=
-\;\frac{1}{D}
\delta(x-x')
\eeq
where $K=\sqrt{s/D}$

\beq
\tilde{g}'' - K^2\tilde{g}= 
-\;\frac{1}{D}
\delta(x-x')
\eeq

homogeneous equation 
and its solution. 
Discontinuous at $x=x'$,
finite for $x\rarrow \infty$
\beq
\tilde{g}(x, x'; s)
=\left\{
\begin{array}{ll}
Ae^{Kx}+ Be^{-Kx}
&\text{if $0<x<x'$}
\\
C e^{-Kx}
&\text{if $x>x'$}
\end{array}
\right.
\eeq
Robin
\beq
K_0 \tilde{g}|_{x=0}+ \tilde{g}'|_{x=0}=0
\eeq
 \beq K_0(A+B) + K(A-B)=0
\eeq

\beq
(K+K_0)A=(K-K_0)B
\eeq

\beq
\boxed
{B=RA, \quad R=\frac{K+K_0}{K-K_0}}
\eeq
\hrule

Let $E=e^{Kx'}$, $\ol{E}=e^{-Kx'}$
Behavior at $x=x'$.
To produce $\delta(x-x')$,
\begin{enumerate}
\item $\tilde{g}$ must be 
continuous at $x=x'$

\beq
\tilde{g}|_{x=x'-\eps}=
\tilde{g}|_{x=x'+\eps}
\eeq

\beq
A e^{Kx'}+ B e^{-Kx'}=
Ce^{-Kx'}
\eeq

\beq
\boxed
{A(E +R\ol{E})-C\ol{E}=0}
\eeq

\item 
$\tilde{g}'$ must jump at $x=x'$

\beq
\int_{x'-\eps}^{x'+\eps}dx\;  [\tilde{g}''-K^2\tilde{g}]= -\;\frac{1}{D}
\eeq
integral over $K^2 \tilde{g}$ is zero as $\eps\to 0$

\beq
\tilde{g}'|_{x=x'+\eps}-
\tilde{g}'|_{x=x'-\eps}=-\;\frac{1}{D}
\eeq


\beq
(-K)Ce^{-Kx'}-(K)Ae^{Kx'}-(-K)Be^{-Kx'}=-\;\frac{1}{D}
\eeq

\beq
\boxed{-C\ol{E} +A(-E+R\ol{E})=-\frac{1}{DK}}
\eeq
\end{enumerate}
\hrule

 
\beq
\underbrace{\begin{pmatrix}
E + R\ol{E}&-\ol{E}
\\
-E + R\ol{E}&-\ol{E}
\end{pmatrix}}_{\calm}
\begin{pmatrix}
A
\\
C
\end{pmatrix}=
\begin{pmatrix}
0
\\
-1/(DK)
\end{pmatrix}
\eeq

\beq
\det\calm=-(1+R\ol{E}^2) -1+R\ol{E}^2=-2
\eeq
\beqa
\begin{pmatrix}
A
\\
C
\end{pmatrix}&=&
\frac{1}{-2}
\begin{pmatrix}
-\ol{E}&\ol{E}
\\
E-R\ol{E}&E+R\ol{E}
\end{pmatrix}
\begin{pmatrix}
0
\\
-1/(DK)
\end{pmatrix}
\\
&=&
\frac{1}{2DK}
\begin{pmatrix}
\ol{E}
\\
E + R\ol{E}
\end{pmatrix}
\eeqa

\beq
A= \frac{\ol{E}}{2DK}, \quad 
B= RA=R\frac{\ol{E}}{2DK},
\quad
C=\frac{E}{2DK}+R\frac{\ol{E}}{2DK}
\eeq

For $0<x<x'$
\beq
\tilde{g}=Ae^{Kx}+Be^{-Kx}=
\frac{1}{2DK}\left[
e^{K(x-x')} +R e^{-K(x+x')}
\right]
\eeq
For $x>x'$
\beq
\tilde{g}=Ce^{-Kx}=
\frac{1}{2DK}\left[ 
e^{-K(x-x')}+Re^{-K(x+x')}\right]
\eeq
so, for all $x$

\beq
\tilde{g}=
\frac{1}{2DK}\left[ 
e^{-K|x-x'|}+R e^{-K(x+x')}\right]
\eeq



\qed

Neumann, $K_0=0$
$\tilde{R}(s)=1$ so 
$R(\tau)=\delta(\tau)$

\beq G^R\rarrow 
g_0(x-x', \tau)
+
g_0(x+x', \tau )
=G^N
\eeq

Dirichlet, $K_0\rarrow \infty$, $\tilde{R}(s)\to -1$ so $R(\tau)=-\delta(\tau)$

\beq G^R\rarrow 
g_0(x-x', \tau)
-
g_0(x+x', \tau -\tau')
=G^D
\eeq



\beq
\call^{-1}_s\left[\frac{1}{s}\exp(-as)\right](t)=
\indi(t>a)
\eeq

\beq
\underbrace{\call^{-1}_s[\tilde{g}_0(a,s)](\tau)}_{g_0(a,\tau)}
=
\call^{-1}_s\left[\left.
\frac{1}{2K}e^{-Kb}
\right|_{K=s}
\right](\tau)
=
\frac{1}{2}\indi(\tau>b)
\eeq

\beq
\call^{-1}_s\left[
\frac{1}{\sqrt{s}}\exp\left(
-a\sqrt{s}\right)\right](t)=
\frac{1}{\sqrt{\pi t}}\exp\left(\frac{-a^2}{4t}\right)
\eeq

\beq
\underbrace{\call^{-1}_s[\tilde{g}_0(b,s)](\tau)}_{g_0(b,\tau)}
=
\call^{-1}_s\left[
\left.
\frac{1}{2DK}e^{-bK}
\right|_{K=\sqrt{s/D}}
\right](\tau)
=
\frac{1}{\sqrt{4\pi D\tau}}\exp\left(
\frac{-b^2}{4D\tau}
\right)
\eeq




\end{document}