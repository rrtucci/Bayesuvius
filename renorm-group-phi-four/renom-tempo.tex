\documentclass[12pt]{report}
\input{../bayesuvius.sty}
\begin{document}

We will use the units 
used in High Energy
Physics in which $\hbar$ (Plank's constant divided by $2\pi$) and $c$ (speed of light) are set to 1 
temporarily, and then restored when one needs actual values in MKS units. In such units,
everything has dimensions of $L^D$, where $L$
stands for length, and $D$ is some rational number. The dimensions of length and time are $L$ and those of energy, mass and
momentum are $L^{-1}$.

Assume the Euclidean metric.
The Euclidean metric can arise
when you analytically
continue a function of spacetime events $x^\mu=(t, x_1, x_2, x_3)$ so that its time
dependence goes from $t$ to $it$. This
as is sometimes useful when integrating that function.
The Euclidean metric can also arise in a
static system that has achieved steady state,
and its time dependence has
gone away. The latter scenario
is the one pertinent to the 
Ising model and its coarse-grained
approximation, the $\phi^4$ theory.

Let

$ \phi(x)$ =  field at point $x\in \RR^d$

$m$ = mass of particle

$\lambda$ = coupling constant


Bare Lagrangian density
in Euclidean metric for
$\phi^4$ theory in $d$ dimensions is
(bare quantities have zero subscript)


\beq
\call_0 =
\frac12 \sum_{\mu=1}^d(\partial_\mu \phi_0)^2
+
\frac12 m_0^2 \phi_0^2
+
\frac{\lambda_0}{4!}\phi_0^4
\eeq


\beq
\engdim{\int d^d x \call_0}
=L^0
\eeq


Let
\beq
\epsilon = 4-d
\eeq
Then define


\beq
\phi_0 = Z_\phi^{1/2}\phi(\mu)
\eeq

\beq
m_0^2 = Z_m m^2(\mu)
\eeq

\beq
\lambda_0 = \mu^\epsilon Z_\lambda \lambda(\mu)
\label{eq-lam-bare-mu-eps}
\eeq

\begin{claim}
\beq
\engdim{\phi_0}=\engdim{\phi}= L^{\frac{2-d}{2}},\quad
\engdim{m^2_0}=\engdim{m^2}=L^{-2}
\eeq
\end{claim}
\proof
\beq
L^0=\engdim{(d^dx) (\partial_\mu \phi)^2}
=
L^d L^{-2} \engdim{\phi}^2
\eeq
\qed

\begin{claim}\label{cl-dim-lam}
The factor $\mu^\epsilon$ 
in Eq.(\ref{eq-lam-bare-mu-eps}) 
is introduced to make the coupling constant $\lam$
 dimensionless and the bare
coupling constant dimensionful.

\beq
\engdim{\lam}=L^0, \quad \engdim{\lam_0}= L^{d-4}
\eeq
\end{claim}
\proof

Assume $\phi^n$
interaction and $\engdim{\lam_0}=L^{-p}$

\beq
L^0=
\engdim{(d^dx)\lam_0 \phi^n_0}=
L^d L^{-p}L^{\frac{n(2-d)}{2}}
\eeq

\beq
0=d-p+ \frac{n(2-d)}{2}
\eeq
\beq
p= d + n -\frac{nd}{2}
\eeq
If $n=4$,

\beq
p=4-d
\eeq
If we now set
$\eps= 4-d$,
we get
\beq
p=\eps
\eeq

In the $\phi^3$
theory in 6 dimensions considered
in Srednicki's book Ref.-------------------%\cite{Sr},
one gets, with $n=3$, that

\beq
p= 3-\frac{d}{2}= \frac{6-d}{2}\eeq
If we now set $\eps =6-d$, we get
\beq
p= \frac{\epsilon}{2}
\eeq
\qed



%\beqa
%\call_0&=&
%Z_\phi\left[
%\frac12
%(\partial_\mu \phi)^2
%+
%Z_m m^2 \phi^2
%+
%\mu^\eps Z_\lam Z_\phi
%\frac{\lambda}{4!}\phi^4
%\right]
%\\
%&=&
%\call + \Delta \call
%\eeqa
%Counterterms $\Delta\call$
%
%\beq
%\Delta \call=
%=\left\{
%\begin{array}{l}
%(Z_\phi-1)
%\frac12
%(\partial_\mu \phi)^2
%\\
%+
%(Z_mZ_\phi -1)m^2 \phi^2
%\\
%+
%(\mu^\eps Z_\lam Z_\phi^2-1)
%\frac{\lambda}{4!}\phi^4
%\end{array}
%\right.
%\eeq

\begin{subequations}
\label{eqs-micro-no-change}
\beq
\frac{d}{d\mu} \phi_0 =0
\eeq
\beq
\frac{d}{d\mu} m_0^2 =0
\eeq
\beq
\frac{d}{d\mu} \lambda_0 =0
\eeq
\end{subequations}
The physical reason 
for Eqs.(\ref{eqs-micro-no-change})
is, in the words of
 Ken Wilson: \qt{The microscopic theory is fixed while the effective couplings change as we change the scale.}


The bare theory does not depend on the arbitrary renormalization scale
$\mu$.
Because the bare parameters are $\mu$-independent, the renormalized ones must \qt{run}.

Define

field anomlous dimension
\beq
\gamma_\phi =
\;\frac{d \ln \phi}{d\ln \mu}
\eeq

 

 mass anomalous dimension

\beq
\gamma_m =
\;\frac{d \ln m^2}{d\ln \mu}
\eeq

beta function\footnote{It's conventional
to define $\beta_q = \frac{dq}{d\ln \mu}$
and $\gamma_q = \frac{d\ln q}{d\ln \mu}$
for any parameter $q$.}

\beq
\beta =
 \frac{d\lambda}{d\ln \mu}
\eeq

\begin{claim}(no wavefunction renormalization at 1-loop)
\beq
Z_\phi = 1 + \calo(\lambda^2)\eeq
\end{claim}
\proof
\qed

\section{$\beta(\lam)$ calculation}
\beqa
\Gamma^{(4)}&=&
Z_\phi^2\left[
\bcen
\xymatrix{
\ar@{-}[dr]
&
&
\\
& \ar@{-}[ur]\ar@{-}[dr]\lam_0
&
\\
\ar@{-}[ur]
&
&
}
\ecen
+
\frac{3}{2}
\bcen
\xymatrix{
\ar@{-}[dr]
&
&
&
\\
&\ar@{-}@/_1pc/[r]\ar@{-}@/^1pc/[r]
\lam_0
& \ar@{-}[ur]\ar@{-}[dr]\lam_0
&
\\
\ar@{-}[ur]
&
&
&
}
\ecen
+\calo(\lam_0^3)
\right]
\\
&=&
Z_\phi^2\left[
\lam_0+  \lam_0^2 \frac{3}{2}\cali
\right]
\eeqa
3 equal one-loop integrals $\cali$, one for each channel (s, t, u)

\beq
\bcen
\xymatrix@C=3pc{
\ar[dr]|{p_1}
&
&
&
\\
&\ar@/_1pc/[r]|{k+p}\ar@/^1pc/@{<-}[r]|{k}
\lam_0
& \ar[ur]\ar[dr]\lam_0
&
\\
\ar[ur]|{p-p_1}
&
&
&
}
\ecen
\cali = \int \frac{d^d k}{(2\pi)^d}
\frac{1}{(k^2 + m^2)((k+p)^2 + m^2)}
\eeq

\begin{mdframed}[hidealllines=true,backgroundcolor=blue!10]
\begin{claim}
\beq
\cali=
\frac{2}{(4\pi)^2}
\left(
\frac{1}{\eps}-b_0
-\ln M(p)
\right)
+\calo(\eps)
\eeq

\beq
\ln M(p)= \frac{1}{2}\int_0^1 dx\;\ln\left(\Delta\right)
\eeq

\beq
b_0=\frac{1}{2}[\gamma_E -\ln(4\pi)]
\eeq

\beq
\Delta = m^2 + x(1-x)p^2
\eeq
\end{claim}
\noindent \proof

Use Feynman parameters:
\beq
\frac{1}{AB} = \int_0^1 dx \frac{1}{[xA + (1-x)B]^2}
\eeq
So:

\beq
\cali = \int_0^1 dx \int \frac{d^d k}{(2\pi)^d}
\frac{1}{\left[(k + xp)^2 + \Delta\right]^2}
\eeq
Shift $k \to k - xp$:

\beq
\cali = \int_0^1 dx \int \frac{d^d k}{(2\pi)^d}
\frac{1}{(k^2 + \Delta)^2}
\eeq
Dimensional regularization integral
Standard formula:

\beq
\int \frac{d^d k}{(2\pi)^d}
\frac{1}{(k^2 + \Delta)^2}
=
\frac{1}{(4\pi)^{d/2}}
\Gamma\left(2-\frac{d}{2}\right)
\Delta^{\frac{d}{2} - 2}
\eeq
Set $d = 4 - \epsilon$:


\beq
\cali=
\frac{1}{(4\pi)^{2-\eps/2}}
\Gamma\left(\frac{\eps}{2}\right)
\int_0^1 dx\;
\Delta^{-\frac{\eps}{2}}
\eeq
Expand everything

\beq
\Gamma\left(\frac{\eps}{2}\right)
\approx
\frac{2}{\eps}-\gamma_E  + \calo(\eps)
\eeq
$\gamma_E \approx 0.58$ Euler constant

\beq
\left(\frac{\Delta}{4\pi}\right)^{-\frac{\eps}{2}}
= 1 -\frac{\eps}{2}\ln\left(\frac{\Delta}{4\pi}\right)+\calo(\eps^2)
\eeq
Multiply pieces

\beqa
\cali&=&
\frac{1}{(4\pi)^2}
\left(
\frac{2}{\eps} -\gamma_E 
-\int_0^1 dx\;\ln\left(\frac{\Delta}{4\pi}\right)
\right)
\\
&=&
\frac{2}{(4\pi)^2}
\left(
\frac{1}{\eps}-b_0
-\frac{1}{2}\int_0^1 dx\;\ln\left(\Delta\right)
\right)
\eeqa

\qed
\end{mdframed}


\begin{claim}
\beq
Z_\lam = 1 + \lam a\left(
\frac{1}{\eps}-b\right) +\calo(\lam^2)
\eeq
where
\beq
a= \frac{3}{(4\pi)^2}\approx 0.0190,
\quad 1/a\approx 52.6
\eeq

\beq
b=
\left\{
\begin{array}{ll}
0 &
\text{if Minimal Subtraction  (MS)}
\\
b_0&
 \text{if Modified Minimal Subtraction  (MMS)}
\end{array}
\right.
\eeq

\end{claim}
\proof

\beq
\underbrace{\mu^\eps}_{1+\eps \ln\mu}\cali=
\frac{2}{(4\pi)^2}\left(
\frac{1}{\eps}-b_0 +\ln\left[\frac{\mu}{M(p)}\right]
\right) + \calo(\eps)
\eeq

\beqa
\Gamma^{(4)}
&=& Z_\phi^2\left[
\lam_0+  \lam_0^2 \frac{3}{2}\cali
\right]
\\
&=&[1+\calo(\lam^2)]
[
\mu^\eps \underbrace{Z_\lam}_{1+\delta Z_\lam} \lam
+
\mu^{ 2\eps} \underbrace{Z^2_\lam}
_{1+\calo(\lam)} \lam^2
\frac{3}{2}\cali
]
\eeqa




\beq
\delta Z_\lam
=\lam\frac{3}{2}
(\mu^{\eps}\cali)_{div}
=\lam\frac{3}{2}
\frac{2}{(4\pi)^2}\left(
\frac{1}{\eps}-b
\right)
=a\lam\left(
\frac{1}{\eps}-b
\right)
\eeq


\qed


\begin{claim}
If
\beq
\left\{
\begin{array}{l}
\lam_0 =
\mu^\eps Z_\lam\tilde{\lam}
\\
\tilde{\lam}=
\lam[1 + ab\lam + \calo(\lam^2)]
\end{array}
\right.
\eeq
or

\beq
\left\{
\begin{array}{l}
\lam_0 =
\tilde{\mu}^\eps Z_\lam\lam
\\
\tilde{\mu}=\mu e^{b}
\end{array}
\right.
\eeq
then $\lam_0$ is independent of $b$.
It equals

\beq 
\lam_0=\mu^\eps \lam
\left[1+
\frac{\lam a}{\eps}
 + \calo(\lam^2)
\right]
\eeq
\end{claim}
\proof

\beqa
\lam_0 
&=&
\mu^\eps \lam
\left[1+
\lam a\left(
\frac{1}{\eps}-b\right)
+ ab\lam + \calo(\lam^2)
\right]
\\
&=&
\mu^\eps \lam
\left[1+
\frac{\lam a}{\eps}
 + \calo(\lam^2)
\right]
\eeqa
\beq
\tilde{\mu}^\eps=
\mu^\eps\left(1 +\eps b
+\calo(\eps^2)\right)
\eeq

\beqa
\lam_0 &=& 
\mu^\eps\left(1 +\eps b
+\calo(\eps^2)\right)
\left[
 1 + \lam a\left(
\frac{1}{\eps}-b\right) +\calo(\lam^2)
\right]\lam
\\
&=&
\mu^\eps
\left(
 1 + \lam a\left(
\frac{1}{\eps}-b\right)
+\lam ab
\right)\lam
\\
&=&
\mu^\eps
\left(
 1 +
\frac{\lam a}{\eps}
\right)\lam
\eeqa

\qed


\begin{claim} 

\beq
\boxed{\beta(\lam)=
-\epsilon \lambda
+a\lam^2}
\eeq


\end{claim}
\proof



\beq
\lam_0=
\mu^\eps \lam
\left[1+\frac{\lam a}{\eps} +\calo(\lam^2)
\right]
\eeq

\beqa
0&=&\mu\frac{d\lam_0}{d\mu}
\\
&=&
\mu\frac{d}{d\mu}
\left(\mu^\eps
\left[\lam+\frac{\lam^2 a}{\eps}
+\calo(\lam^3) 
\right]\right)
\\
&=&
\eps\mu^\eps
\left[\lam+\frac{\lam^2 a}{\eps}
\right]
+
\mu^\eps \beta(\lam)
\left[1+\frac{2\lam a}{\eps}
\right]
+
\calo(\lam^3)
\eeqa

\beqa
\beta(\lam)
&=&
-\eps\lam
\frac{1+\frac{\lam a}{\eps}}
{1+\frac{2\lam a}{\eps}} +\calo(\lam^3)
\\
&=&
-\eps\lam\left(
1-\frac{\lam a}{\eps} 
\right)+\calo(\lam^3)
\\
&=& -\eps\lam +a \lam^2
\eeqa
\qed


Set
\beq
\beta(\lambda_*) =0
\eeq

This gives the {\bf Wilson–Fisher fixed point}

\beq
\lambda_* =
\frac{\epsilon}{a}
\approx 52.6 \eps
\eeq

For $d=4$, 
$
\epsilon=0
$
so the fixed point is Gaussian.
For $d<4$ a non-trivial fixed point appears.
This fixed point describes the {\bf Ising universality class}

\section{$\gamma_m$ calculation}




 One-loop correction to 
2-point function, self-energy (tadpole diagram)

\beqa
\Gamma^{(2)}&=&
Z_\phi
\left[
\bcen
\xymatrix{
\ar@{-}[r]
&\ar@{-}[r]m_0^2
&
}
\ecen
+
\frac12
\bcen
\xymatrix{
&
&
\\
\ar@{-}[r]
&\ar@{-}[r]
{\lam_0}\loopup{6}{@{-}}
&
}
\ecen
\right]
\\
&=&
Z_\phi \left[
Z_m m^2 + \underbrace{\frac12 \lam \mu^\eps Z_\lam \calt}
_{\Sigma}
\right]
\eeqa

\beq
\bcen
\xymatrix{
&
&
\\
\ar@{-}[r]
&\ar@{-}[r]
{\lam_0}\loopup{6}{|k}
&
}
\ecen
\quad
\calt = 
\int \frac{d^d k}{(2\pi)^d}\;
\frac{1}{k^2 + m^2}
\eeq

\begin{mdframed}[hidealllines=true,backgroundcolor=blue!10]
\begin{claim}
\beq
\calt =
\frac{m^2}{(4\pi)^2}
\left[
-\frac{1}{\epsilon}
-b'_0 
+ \frac12\ln m^2
  \right]+ \calo(\epsilon)
\eeq

\beq
b_0= 1+\frac12 \ln(4\pi)
\eeq
\end{claim}
\noindent \proof

\beq
\calt=
\frac{1}{(4\pi)^{d/2}}
\Gamma\left(1 - \frac{d}{2}\right)
(m^2)^{\frac{d}{2} - 1}
\eeq
Set $d = 4 - \epsilon$

\beq
\calt=
\frac{1}{(4\pi)^{2-\eps/2}}
\Gamma\left({\color{red}-1}+\frac{\eps}{2}\right)
(m^2)^{{\color{red}1}-\frac{\eps}{2}}
\eeq
\beq
\Gamma\left(-1 + \frac{\eps}{2}\right)
= -\frac{1}{\epsilon} - 1 + \calo(\epsilon)
\eeq

Also:
\beq
(m^2)^{1 - \frac{\epsilon}{2}}
= m^2 \left(1 - \frac{\epsilon}{2}\ln m^2\right)
\eeq

And:
\beq
(4\pi)^{-2 + \frac{\epsilon}{2}}
= \frac{1}{(4\pi)^2}\left(1 + \frac{\epsilon}{2}\ln(4\pi)\right)
\eeq

Putting it all together:



\beq
\calt =
\frac{m^2}{(4\pi)^2}
\left[
-\frac{1}{\epsilon}-1
-\frac12\ln(4\pi)
+ \frac12\ln m^2
  \right]+ \calo(\epsilon)
\eeq

\qed
\end{mdframed}

\begin{claim}
\beq
Z_m =
1 + a'\lam
\left(\frac{1}{\epsilon}+b'\right)
\eeq
\beq
a'=\frac{1}{2(4\pi)^2}
\eeq

\beq
b'=
\left\{
\begin{array}{ll}
0 &
\text{if Minimal Subtraction  (MS)}
\\
b'_0&
 \text{if Modified Minimal Subtraction  (MMS)}
\end{array}
\right.
\eeq
\end{claim}
\proof

\beqa
\underbrace{\mu^\eps}_{1+\eps \ln\mu}\calt
&=&
\frac{m^2}{(4\pi)^2}
\left(
-\frac{1}{\eps}-b'_0 
+\frac12 \ln m^2\right)
+\calo(\eps)
\\
&=&
\frac{m^2}{(4\pi)^2}
\left(
-\frac{1}{\eps}-b'_0 
+\ln \frac{m}{\mu}\right)
+\calo(\eps)
\eeqa

\beqa
\Gamma^{(2)}
&=&
Z_\phi \left(
Z_m m^2 + \frac12 \lam \mu^\eps Z_\lam \calt
\right)
\\
&=&
\left(1 +\calo(\lam^2)\right)
\left(
\underbrace{Z_m}_{1 +\delta Z_m}
 m^2 + \frac12 \lam \mu^\eps 
\underbrace{Z_\lam} _{1 +\calo(\lam)}
\calt
\right)
\eeqa

\beq
m^2\delta Z_m=
-\;\frac12(\lam\mu^\eps \calt)_{div}
=
\frac{m^2}{2(4\pi)^2}\lam
\left(
\frac{1}{\eps}+ b'\right)
\eeq

\qed


\begin{claim}
\beq
\boxed{
\gamma_m(\lambda)
=
a'\lam
+\calo(\lambda^2)}
\eeq
\end{claim}
\proof

\beq
0= \frac{d\ln(m_0^2)}{d\ln\mu}  = \frac{d\ln Z_m}{d\ln\mu} + \frac{d\ln (m^2)}{d\ln \mu}
\eeq

\beq
\gamma_m=
-\; \frac{d \ln Z_m }{d\ln\mu}
\eeq

\beqa
\gamma_m &=&- \beta(\lam)\frac{d}{d\lam}
\underbrace{\ln Z_m}_{a'\lam
\left(\frac{1}{\epsilon}+b'\right)}
\\
&=&
-(-\epsilon \lambda
+a\lam^2)\frac{a'}{\eps}
\\
&=&
a'\lam +\calo(\lam^2)
\eeqa

\qed



Important consistency checks


$\gamma_m$ is dimensionless


Positive → mass grows in IR  

Scheme independence (1-loop)
Same in MS and MMS


\section{Critical Exponents}

The critical point of the $\phi^4$ theory and that of the Ising model
are
in the same universality class.

One loop results
\beq
\left\{
\begin{array}{ll}
\beta(\lam) = -\eps \lam +a \lam^2 + \calo(\lam^3), & (a= \frac{3}{(4\pi)^2})
\\
\gamma_m(\lam)=a'\lam +\calo(\lam^2), & (a'= \frac{1}{2(4\pi)^2})
\\
\gamma_\phi = 0 +\calo(\lam^2)
\end{array}
\right.
\eeq

\begin{claim}
\beq
\nu=
\frac{1}{2}+\frac{a'\eps}{4a} + \calo(\eps^2)
\eeq
\end{claim}
\proof

Near the critical point

\beq
m^2 \propto \mu^2 \underbrace{
\left(\frac{T-T_c}{T_c}\right)}_{\theta}
\eeq
so the mass squared parameter is proportional  to the temperature deviation times $mu^2$ so so that it scales as $\mu^2$ or $b^2$.

At any critical point,
the correlation length $\xi$ diverges as a power $\nu$
of $1/|\theta|$.

\beq
\xi = \frac{1}{|\theta|^\nu}\xi_0
\eeq
Since also $\xi = \xi_0/b$, it is convenient 
to define 

\beq
b= |\theta|^{-\nu},
\quad
\frac{d\ln | \theta|}{d\ln b}= 
y_\theta = \;\frac{1}{-\nu}
\eeq
and $d\ln\mu = d\ln b$ so

\beq
\gamma_m=\frac{d\ln m^2}{d\ln\mu}= 2 - \frac{1}{\nu}
\eeq

\beq
\nu = \frac{1}{2 - \gamma_m}
\eeq


\beq
\lam^* = \frac{\eps}{a}
\eeq

\beq
\gamma_m(\lam^*)= a'\lam^* = a'\frac{\eps}{a}
\eeq

\beqa
\nu &=&\frac{1}{2 - \frac{a'\eps}{a}}
\\
&=& \frac{1}{2\left(1 -\frac{a'\eps}{2a}\right)}
\\
&=&
\frac{1}{2}+\frac{a'\eps}{4a} + \calo(\eps^2)
\eeqa
\qed

\begin{claim}
\beq
\eta = \gamma_\phi(\lam^*)
= 0 +\calo(\eps^2)
\eeq
\end{claim}
\proof

At the critical point, the connected Green function $G_c$ scales\footnote{When I say $q$ scales as $b^{y_q}$
or write $q\sim b^{y_q}$, I mean $q(b) = b^{y_q}q_0$} as

\beq
G_c\propto \frac{1}{r^{d-2+\eta}}\sim b^{d-2+\eta}
\eeq
Also

\beq
\engdim{\phi}=M^{\frac{d-2}{2}}
\eeq
Hence

\beq
G_c = \av{\phi, \phi} \sim b^{d-2+2y_\phi}
\eeq

Hence,

\beq
\eta=2y_\phi
\eeq
Now use

\beq y_\phi=\gamma_\phi = 0 +\calo(\lam^2)
\eeq


\qed


\section{Coupling Constant behavior near fixed points}



\beq
\int_{\lam_0}^\lam\frac{d\lam}{\beta(\lam)}=\int_{\mu_0}^\mu d\ln(\mu)
\eeq

\beqa
\beta(\lam) &=& -\eps\lam + a\lam^2 +\calo(\lam^3)
\\
&=&
a\lam(\lam-\lam^*)
\quad \text{with } \lam^*=\frac{\eps}{a}
\eeqa

\beq
\beta(\lam)= a(\lam-\lam^*_1)(\lam-\lam^*_2)\ldots(\lam-\lam^*_n)
\eeq

\beq
\beta(\lam)\approx (\lam-\lam^*_1)
\underbrace{a(\lam^*_1-\lam^*_2)\ldots(\lam^*_1-\lam^*_n)}_R
\eeq

\beq
\ln\frac{\lam-\lam_1^*}
{\lam_0-\lam_1^*}
=R
\ln\frac{\mu}{\mu_0}
\eeq



%https://en.wikipedia.org/wiki/Landau_pole

For both QED and QCD,\footnote{The Wikipedia article on Landau Poles asserts that Landau in 1954 assumed
$\beta(g)= \beta_2 g^2 +\calo(g^3)$. This is historically correct but not the modern usage. Landau used $g=\frac{e^2}{4\pi}$ where $e$ is the charge of the electron. We are using $g=e$ instead.  } 
\beq
\beta(g)= \beta_3 g^3 +\calo(g^4)
\eeq

\beq
\frac{dg}{d\ln \Lam}=\beta(g)\implies
\int^g_{g_0}\frac{dg}{\beta_3 g^3}= \int_{\Lam_0}^\Lam d\ln \Lam
\eeq

\beq
-\;\frac{1}{ 2g^2}+\frac{1}{ 2g^2_0}=\beta_3\ln \frac{\Lam}{\Lam_0}
\eeq

\beqa
g^2 &=& \frac{1}{\frac{1}{g^2_0} -2\beta_3\ln\frac{\Lam}{\Lam_0}}
\\
&=&
\frac{g^2_0}{1 - 2\beta_3g^2_0\ln\frac{\Lam}{\Lam_0}}
\label{eq-g-landau-pole}
\eeqa

A fixed point $g^*$ satisfies
$\beta(g^*)=0$. Hence
both QED and QCD have a 
unique fixed point (as far
as we can tell from
perturbation theory
(in powers of $g$,  at $g=0$).


\begin{figure}[h!]
\centering
\includegraphics[width=5in]
{landau-pole.png}
\caption{Plots of
$g(\Lam/\Lam_0)$ defined by Eq.(\ref{eq-g-landau-pole})
for $\beta_2=\pm 2.3$ and $g_0=1$.}
\label{fig-landau-pole}
\end{figure}

Landau Pole (LP)
\beq
\frac{\Lam_{LP}}{\Lam_0}=
\exp\left(\frac{1}{2\beta_3g^2_0}\right )
\eeq

$\Gamma=\Lam_{LP}$ marks a scale,
not the position
of a fixed point as does $\Lam=0$.

LP only arises for
QED, not for QCD (asymptotic
freedom of QCD ($g\downarrow$ as
 $\Lam\uparrow$
is incompatible with the prediction of a LP).

For QED, one takes $\Lam_0 = m_0$, the bare mass of the electron and $g_0$ equal to
the bare coupling.

For QCD, if it the LP 
rationale were valid,
one would take $m_0=$ bare mass if a quark. But if we assume $m_0\rarrow 0$, and $\beta_3<0$,
then, $\Lam_{pole}\rarrow 0$.


For QED, perturbation in powers of $g$
works only for low $\Lam$.
For QCD, perturbation in powers of $g$
works only for high $\Lam$.



\begin{figure}[h!]
\centering
\includegraphics[width=5in]
{qcd-qed-g.png}
\caption{Plots of
$g(\Lam/\Lam_0)$ 
for QCD ($\beta_2<0$)
and QED ($\beta_2>0$).
For QCD, perturbation theory breaks down
for $0\leq\Lam/\Lam_0<<1$
so this figure is incorrect but suggestive. In reality, $\Lam_{pole}/\Lam\rarrow 0$.}
\label{fig-qcd-qed-g}
\end{figure}



%
%\beq
%\frac{d\lam}{d\ln\mu}=a\lam^n
%\eeq
%
%For $n=1$,
%
%\beq
%\frac{1}{a}\ln\left(\frac{\lam(\mu)}{\lam(\mu_0)}\right)=
%\ln\left(\frac{\mu}{\mu_0}\right)
%\eeq
%Define
%\beq
%K(\lam, \mu)= \frac{1}{a}\ln\left(\frac{1}{\lam(\mu)}\right) +\ln\mu
%\eeq
%
%For $n>1$
%\beq
%\frac{1}{\lam^{n-1}(\mu)}
%-
%\frac{1}{\lam^{n-1}(\mu_0)}
%=
%-(n-1)a\ln\left(\frac{\mu}{\mu_0}\right)
%\eeq
%Define
%\beq
%K(\mu, \lam)=\frac{1}{(n-1)a \lam^{n-1}(\mu)} +\ln \mu
%\eeq
%
%\beq
%K(\lam,\mu)=K(\lam_0, \mu_0)
%\eeq
%
%\begin{figure}[h!]
%\centering
%\includegraphics[width=5.9in]
%{running-coupling.png}
%\caption{Running Coupling constant $\lam(\mu)$
%for $\beta=a\lam^n$ for
%cases $n=1$ and $n>1$.}
%\label{fig-running-coupling}
%\end{figure}

\section{Beta Potential}

Newton's equation says 
\beq
m\ddot{x}= F
\eeq
where $m=$mass, $x=$ position, and $F=$ force. But
you could  have, once the acceleration dies down,
an equilibrium between a drag force and a propulsion force. Let

$\gamma \dot{x}=$ drag,  a velocity dependent force with $\gamma>0$

$F=-\partial_x V=$ propulsion, a conservative force with potential function $V$

Then, at equilibrium,

\beq \gamma\dot{x}=F = -\partial_x V
\label{eq-phi-four-drag-prop}
\eeq

Note that

\beq
\gamma\partial_x\dot{x}=F = -\partial_x^2 V
\eeq


As shown in Fig.\ref{fig-V-derivatives-phi-four},
$V$ has positive second derivative
(hold water convexity)
on both sides of a {\bf stable, attractive fixed  point (attractor, sink)},
and it has negative second derivative
(drop water convexity)
on both sides of an {\bf unstable, repulsive fixed point (repeller, source)}. Hence, we can think of
$V$ as a potential in which a ball rolls towards an attractor
and away from a repeller.



\begin{figure}[h!]
\centering
\includegraphics[width=4in]
{../dynamical-sys/V-derivatives.png}
\caption{Illustration of
$\frac{d\dot{x}}{dx}=-\partial^2_xV$. Arrows
in vector field are 
proportional to $\dot{x}$. }
\label{fig-V-derivatives-phi-four}
\end{figure} 

Now 
set
position =$x\rarrow \lam$, time=$t\rarrow \ln \mu$,
and $\gamma=1$
in Eq.(\ref{eq-phi-four-drag-prop})

\beq
\frac{d \lam}{dt}= \beta(\lam) = -\partial_\lam V
\eeq

\beq
V(\lam)-V(\lam_0)
=
-\int_{\lam_0}^\lam
d\lam\; \beta(\lam)
\eeq

\beq
\beta(\lam)= -\eps \lam + a\lam^2
\implies V(\lam)= \frac{\eps}{2}\lam^2-
\frac{a}{3}\lam^3 + \text{constant}
\eeq

\begin{figure}[h!]
\centering
\includegraphics[width=3in]
{beta-potential.png}
\caption{Beta  potential curve $V(\lam)$.
$time=\ln b= \ln(1/\Lam$). As  $b$ or $\Lam$ increases, a ball rolls downward in this potential.}
\label{fig-beta-potential}
\end{figure}

Minimum at $\lam=0$ and maximum at

\beq
0=\partial_\lam V(\lam^*)=-\beta(\lam^*)
\implies \quad
\lam^*=\frac{\eps}{a},
\quad V(\lam^*)=\frac{\eps^3}{6a^2}
\eeq
Wilson-Fisher fixed point (Curie point).

Recall

\beq
\ln b = \ln \mu =\ln\left(\frac{1}{\Lam}\right) 
\eeq

larger $b$ (coarser grid) $\implies$ smaller $\Lam$ (UV energies)

smaller $b$ (finer grid) $\implies$ larger $\Lam$ (Infrared energies)


 QED (Quantum Electrodynamics)
has $\beta>0$ which gives it
an infrared fixed point,
whereas QCD (Quantum Chromodynamics)
has $\beta<0$ which gives it an ultraviolet
fixed point.

\section{$\phi^4$-Ising connection}

Start with the Ising model partition function (set $H=0$ for now):

\beq
Z = \sum_{{S_i=\pm1}} \exp\left(\beta J \sum_{(i, j)\in NN} S_i S_j
+\beta H\sum_i S_i
\right)
\eeq

\begin{claim}(Ginzburg-Landau model)
\beq
Z= \int \cald\phi\; e^{-\cala}
\eeq
where
the action $\cala$ is given by

\beq
\cala[\phi] \approx \int d^2 x \left[\frac12 \sum_{\mu=1}^2
(\partial_\mu \phi)^2 + \frac{m^2}{2}\phi^2 + \frac{\lam}{4!}\phi^4
+ h\phi\right]
\eeq

\end{claim}
\proof


set $H=0$ to begin with.


Rewrite the interaction in matrix form:

\beq
\beta J\sum_{(i,j)\in NN} S_i S_j = \frac12 \sum_{i, j} S_i K_{ij} S_j =\frac12 S^T K S
\eeq

\beq
\begin{array}{ccc}
\bcen
\xymatrix@R=1pc@C=1pc{
S_1
&S_2
&S_3
\\
S_4
&S_5
\ar@{-}[r]
\ar@{-}[l]
\ar@{-}[u]
\ar@{-}[d]
&S_6
\\
S_7
&S_8
&S_9
}
\ecen
&\rarrow
&
\bcen
\xymatrix@R=1pc@C=.1pc{
&\scriptstyle(1+a\partial_y+ \frac{a^2}{2}\partial^2_y)S_5
&
\\
\scriptstyle(1-a\partial_x+ \frac{a^2}{2}\partial^2_x)S_5
&S_5
\ar@{-}[r]
\ar@{-}[l]
\ar@{-}[u]
\ar@{-}[d]
&\scriptstyle (1+a\partial_x+ \frac{a^2}{2}\partial^2_x)S_5
\\
&\scriptstyle(1-a\partial_y+ \frac{a^2}{2}\partial^2_y)S_5
&
}
\ecen
\\
\\
\beta J S_5(S_4 +  S_6 
+ S_8 + S_2)
&\approx
&
\beta J S_5 \left[
4 +a^2(\partial_x^2 + \partial_y^2)
\right]S_5
\end{array}
\eeq


\beq
\frac{K}{2}= 4\beta J (1 +\frac{a^2}{4}\sum_\mu \partial_\mu^2 )
\implies K^{-1}=\frac{1}{8\beta J}
\frac{1}{1 +\frac{a^2}{4}\sum_\mu \partial_\mu^2 }\approx
\frac{1}{8\beta J}\left(
1 -\frac{a^2}{4}\sum_\mu \partial_\mu^2\right)
\eeq

\beq
\int_{-\infty}^\infty
dx\;e^{-a x^2 + bx}=\sqrt{\frac{\pi}{a}}
e^{\frac{b^2}{4a}}
\eeq



\beq
\int \underbrace{\cald\phi}_{\prod_i d\phi_i} \;
\exp\left(-\;\frac{1}{2}\phi^T K^{-1}\phi + \phi^T S\right)\propto
e^{\frac{1}{2} S^T K S} 
\eeq

\beq
Z = \int \cald \phi \;
e^{-\frac12\phi^T K^{-1}\phi}
\prod_i \sum_{S_i=\pm1} e^{\phi_i S_i}
\eeq

\beq
\sum_{S_i=\pm1} e^{\phi_i S_i} = 2\cosh(\phi_i)
\eeq

\beq
Z = \int \cald\phi \;
\exp\left[
-\;\frac{1}{2}\phi^T K^{-1}\phi + \sum_i \ln(2\cosh \phi_i)
\right] 
\eeq

Near criticality, $ \phi $ is small, so expand:

\beq
\ln (2\cosh \phi_i) =
\ln(2) +\frac{\phi_i^2}{2} - \frac{\phi_i^4}{12} + \calo(\phi_i^6)
\eeq

\beq
Z= \int \cald\phi\; e^{-\cala}
\eeq

\beq
\cala [\phi] = 
\frac{1}{2}\sum_{i,j}\phi_i (K^{-1})_{i,j}\phi_j -
  \sum_i \left(
  \frac{\phi_i^2}{2} - \frac{\phi_i^4}{4(3)} + \cdots
  \right)
\eeq


Now take continuum limit:

\beq
\sum_i \rarrow \int \frac{d^2x}{a^2}
\eeq





\beqa
\cala[\phi] &=& \int \frac{d^2 x}{a^2} \left[\frac{\phi}{16\beta J}\left(
1 -\frac{a^2}{4}\sum_\mu \partial_\mu^2\right)\phi-\frac{1}{2}\phi^2 + \frac{1}{4(3)}\phi^4 
\right]
\\
&=&
\int d^2 x \left[-\;\frac{\phi}{8^2\beta J}\left(
\sum_\mu \partial_\mu^2\right)\phi
+\left(\frac{1}{a^2 8\beta J}
-\frac{1}{a^2}\right)\frac{\phi^2}{2} + 
\left(\frac{2}{a^2}\right)\frac{\phi^4}{4!} 
\right]
\eeqa
 Integrate by parts first term
to replace $\phi\partial^2_\mu\phi$ by $-(\partial_\mu \phi)^2$.
Then set
\beq
\lam = \frac{8}{a^2}\eeq

\beq
m^2 = 
\frac{4}{a^2}
\left(
\frac{1-8\beta J}{8\beta J}
\right)
\eeq


\beqa
\cala[\phi]
&=&
\frac{1}{4}
\int d^2x\;
\left[
\frac{1}{(8\beta  J)2}
\sum_\mu\left(\partial_\mu\phi\right)^2 + m^2 \frac{\phi^2}{2}
+ \lam \frac{\phi^4}{4!}
\right]
\eeqa 

\beq
T_c = 8J,\quad 8\beta J = T_c/T
\eeq
Set $8\beta J\approx 1$ in first term.
Rescale $\phi = 2\phi'$, $\lam= \lam'/4$

\beq
\boxed{
\lam' = \frac{32}{a^2}}
\eeq

\beq
\boxed{m^2 = \frac{4}{a^2}
\left(
\frac{T-T_c}{T_c}
\right)}
\eeq

\beq
\cala[\phi']=
\int d^2x\;
\left[
\frac12 \sum_\mu(\partial_\mu  \phi')^2
+\frac{m^2}{2}(\phi')^2
+\frac{\lam'}{4!}(\phi')^4
\right]
\eeq


If you include  $H$ term in $Z$,
\beq
\ln[2\cosh \phi_i]
\rarrow \ln[2\cosh(\phi_i + \beta H)]
\eeq

\beqa
\ln[2\cosh(\phi_i + \beta H)]
&\approx& \ln(2) + \frac12 (\phi_i + \beta H)^2
\\
&\approx&\ln(2) +  \frac{\phi^2_i + (\beta H)^2}{2} + \beta H  \phi_i
\\
&\approx&\ln(2) +  \beta H \phi_i
\eeqa


\beqa
\sum_i \ln[2\cosh(\phi_i + \beta H)]
&\rarrow&\int \frac{d^2 x} {a^2}\;\beta H \phi
\\
&\rarrow&\int \frac{d^2 x} {a^2}2\beta H \phi\quad \text{(from $\phi\rarrow 2\phi$ rescaling)}
\\
&=&
\int d^2x\; h \phi
\eeqa

\beq
\boxed{h=\frac{2\beta H}{a^2}}
\eeq
\qed

 $$
S_i \to -S_i \Rightarrow \phi \to -\phi
 $$


  $ \phi^4 $ is   term is r-unstable and marginal,
  higher powers are   i-stable.



Even though Ising has no explicit $ S^4 $ term because
 $
s_i^4 = 1
 $
coarse-graining produces   nontrivial fluctuations, and those produce a genuine $ \phi^4 $ interaction.

  Ising and $\phi^4$ model exists in any $d$. dimension $d=4$
  special for $\phi^4$:
 the   upper critical dimension 



From Claim \ref{cl-dim-lam},
$\engdim{\lam}=L^{4-d}$

\begin{itemize}
 \item $ d < 4 $:
  $ \lam $ has   positive dimension   so $\phi^4$ term is  r-unstable.
  $\lam^p$
  times integral
implies integral $\sim L^{p(d-4)}$
so converges.   fluctuations matter, nontrivial critical behavior

\item  $ d = 4 $:
  $ \lam $ is   dimensionless  so $\phi^4$ term is marginal. $\lam^p$ times integral
usually converges

\item  $ d > 4 $:
  $ \lam $ has negative dimension  so $\phi^4$ term is i-stable.
$\lam^p$ times integral 
diverges. 
  fluctuations die, mean-field theory works
\end{itemize}





 So why 2-dim Ising model works 2-dim $ \phi^4 $?


d-dim Ising model coarsening of d-dim $ \phi^4 $. we expand around $d=4$ because
  theory is weakly coupled near $ d=4 $ and allows
  allows perturbation theory

Then extrapolate to:

  $ d=3 $ (good)
  $ d=2 $ (qualitatively OK, quantitatively rough)



\section{Spontaneous Symmetry Breaking}

Let

$r=m^2=\propto T-T_c$

$u=\lam$

variables are named this way 
because Curie-Fisher-Wilson point at $r=r^*$ and $u=0$
is attractive (i.e., i-stable)
in the $u$ direction and repulsive (i.e., r-unstable) in the $r$ direction.


The Ginzburg–Landau free energy (= Euclidean Action  $\cala$)

\beq
F[\phi] = \int d^d x \left[ \frac{1}{2}\sum_\mu(\partial_\mu \phi)^2 + \frac{r}{2}\phi^2 + \frac{u}{4!}\phi^4 \right]
\eeq

\beq
V(\phi)= \frac{r}{2}\phi^2 + \frac{u}{4!}\phi^4
\eeq


\beq
V'(\phi)=0\implies\quad \phi=0 \text{ or } \phi=\phi^*=\sqrt{\frac{-6r}{u}}
\eeq

Some people define $u=\lam/6$ in which case $\phi^* =\sqrt{\frac{-r^*}{u}}$.

\begin{figure}[h!]
\centering
\includegraphics[width=2.3in]
{phi-potential.png}
\caption{$\phi$  potential curve $V(\phi)$.}
\label{fig-phi-potential}
\end{figure}

\begin{figure}[h!]
\centering
\includegraphics[width=4.5in]
{r-u-phases.png}
\caption{$(r,u)$ plane exhibiting Curie-Fisher-Wilson fixed point (saddle-point) at $(r,u)=(0, u^*)$, and a trivial fixed point (maximum) at $(r,u)=(0,0)$.
Top-right quadrant harbors the disordered phase, and  top-left quadrant harbors 2 possible spontaneous phases (all spins up or all spins down). $r=m^2\propto (T-T_c)$ is a temperature-like c.c. so r-unstable, repulsive}
\label{fig-r-u-phases}
\end{figure}

 In the $(r,u)$ plane:
 The region $r>0, u>0$ corresponds to the disordered phase
 and the region $r<0, u>0$ corresponds to the ordered phase with a Mexican hat potential.
 The Wilson–Fisher fixed point lies at $r=0, u=u^*$.
 The separation between minima $\Delta \phi \sim \sqrt{|r|/u}$ is constant along lines $u = a r$ with $a<0$.

\begin{figure}[h!]
\centering
\includegraphics[width=5in]
{qcd-qed-r-u-plane.png}
\caption{$(r, u)$ plane 
for QCD ($\beta_2<0$)
and QED ($\beta_2>0$).
The Landau pole $(0, u_{LP})$ is a pure repeller for for both QED and QCD.}
\label{fig-qcd-qed-r-u-plane}
\end{figure}

For QCD and QED, $r=m^2_f$, fermion mass (mass of electron for QED and mass of quark for QCD). 

\end{document}